import { NombreCortoPipe } from './nombre-corto.pipe';

describe('NombreCortoPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreCortoPipe();
    expect(pipe).toBeTruthy();
  });
});
