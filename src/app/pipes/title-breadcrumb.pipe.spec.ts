import { TitleBreadcrumbPipe } from './title-breadcrumb.pipe';

describe('TitleBreadcrumbPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleBreadcrumbPipe();
    expect(pipe).toBeTruthy();
  });
});
